# VisualMap PRD

## 1. 产品概述

VisualMap 是一个把长内容转化为可点击视觉知识地图的产品。

用户不需要进入一个传统网站首页，也不需要先看到导航栏、介绍文案、功能卡片或营销式落地页。用户打开 VisualMap 后，第一眼应该看到的是一个已经铺开的、完整渲染的视觉画面：它像一张可交互的知识海报、研究地图或动态信息图，而不是一个网页表单。

产品支持两种入口：

```text
入口 1：把链接直接拼在 VisualMap URL 后，打开即生成视觉地图
入口 2：进入输入框模式，粘贴链接或正文后生成视觉地图
```

VisualMap 的核心体验是：用户输入一篇文章、视频、PDF、论文或其他长内容后，系统自动理解结构，并生成一张可点击的视觉知识地图。用户点击地图中的节点后，可以继续下钻查看解释、证据、相关背景、个人批注或分享版本。

## 2. 产品定位

一句话定位：

```text
把任意长内容变成一个打开即呈现、可点击下钻、可批注分享的视觉知识地图。
```

VisualMap 不是传统阅读器，不是聊天工具，也不是普通网页摘要工具。

它更接近：

```text
Perplexity 的可信引用
+ Pinterest 的视觉吸引力
+ HyperCard 的点击跳转
+ 可视化 RAG 的结构化理解
+ 一张 fancy 的可交互知识海报
```

产品的第一性原则是：入口尽可能轻，画面尽可能完整，信息尽可能可验证，探索尽可能有惊喜。

## 3. 核心目标

### 3.1 用户目标

- 快速理解一篇长内容的整体结构。
- 通过视觉地图找到自己真正关心的部分。
- 在可信解释和开放探索之间自由切换。
- 给节点增加自己的评论、批注和疑问。
- 把生成好的地图分享给别人，让别人只读查看和下钻。

### 3.2 产品目标

- 降低长内容理解门槛。
- 把线性阅读变成空间化探索。
- 避免传统网站式界面，让用户打开即进入内容画面。
- 通过 Explain / Explore / Add / 分享模式解决可信、延展、沉淀和传播的问题。

## 4. 非目标

第一阶段不做：

- 传统网站首页。
- 营销式 hero page。
- 多页文档站结构。
- 复杂账号系统和团队空间。
- 多人实时协同编辑。
- 完全开放、无来源约束的无限探索。
- 自动执行网页操作。
- 把全部文字渲染进 AI 图片里。

## 5. 入口设计

### 5.1 URL 直达生成

用户可以把原始内容链接直接拼到 VisualMap 的 URL 后。

示例：

```text
https://visualmap.app/https://example.com/article
```

或使用更稳定的 query 参数形式：

```text
https://visualmap.app/?url=https%3A%2F%2Fexample.com%2Farticle
```

推荐第一版优先支持 query 参数形式，因为它更容易处理特殊字符、短链、中文 URL 和埋点。

用户打开后不进入首页，而是直接进入生成流程：

```text
打开 URL
  -> 识别目标链接
  -> 抓取和解析内容
  -> 展示生成中的视觉画面
  -> 自动切换为完整视觉地图
```

生成过程中也不应该展示传统 loading 页面，而应该展示一个逐渐成形的视觉画布。例如：

- 背景先出现。
- 主标题和中心节点浮现。
- 章节节点逐步展开。
- 证据节点和路径线逐步连接。
- 右侧细节层在需要时滑出。

### 5.2 输入框模式

如果用户没有带链接访问，页面直接进入一个极简输入框模式。

这个输入框模式仍然不应该像传统网页表单。它应该像一张空白地图的中心入口：

```text
中心是一个发光输入框或命令条
背景是待生成的空画布
输入后画面从中心向外展开
```

支持输入：

- 文章链接。
- 粘贴正文。
- PDF 链接。
- 视频链接。
- 未来扩展：GitHub 仓库、播客链接、会议纪要链接。

输入框模式的主行为：

```text
用户输入内容
  -> 回车或点击生成
  -> 输入框收缩成地图标题
  -> 生成完整视觉地图
```

## 6. 核心体验形态

VisualMap 的默认形态不是“网页”，而是“渲染画面”。

打开产品后，用户应该看到：

```text
全屏视觉画布
  + 可点击知识节点
  + 视觉化路径连接
  + 少量浮层控制
  + 节点详情抽屉
```

界面不应该出现明显的网站栏目感，例如大导航栏、页脚、功能介绍区、价格区、博客入口等。

### 6.1 画布

画布是产品主体，占据第一视觉层级。

画布可以根据内容类型生成不同视觉结构：

- 文章：论点地图、证据树、概念网络。
- 视频：时间线地图、关键帧地图、观点流。
- 论文：研究问题、方法、实验、结论、局限性。
- 报告：指标图谱、趋势图、结论和风险。
- 代码仓库：模块图、调用链、文件结构和关键流程。

### 6.2 节点

节点是地图里的可点击对象。

节点类型包括：

- 总览节点。
- 章节节点。
- 核心论点节点。
- 证据节点。
- 原文引用节点。
- 背景概念节点。
- 争议节点。
- 用户批注节点。

节点点击后，用户进入下一层画面，而不是打开一个传统详情页。

### 6.3 详情抽屉

详情抽屉用于承载文字、引用、批注和操作。

它应该是辅助层，不是主界面。默认情况下，地图仍然是主角。

详情抽屉内容根据模式变化：

- Explain：原文解释、引用、证据链、确定性等级。
- Explore：相关背景、类比、争议、外部来源、继续探索问题。
- Add：用户评论、批注、标签、疑问、收藏。
- 分享模式：只读查看公开内容和公开批注。

## 7. 模式设计

### 7.1 Explain 模式

Explain 模式基于原始内容，稳定、可验证地往下钻。

它回答：

```text
原文说了什么？
证据在哪里？
这个观点怎么推出来？
这个概念在上下文里是什么意思？
```

要求：

- 每个结论必须能回到原文。
- 引用必须显示段落、页码、时间戳或文件位置。
- 不允许把外部推测混入 Explain 结论。

### 7.2 Explore 模式

Explore 模式在原始内容之外，向相关可能性、延展信息、类比、争议和外部世界探索。

它回答：

```text
这件事还能连接到哪里？
有没有相似案例？
有哪些反方观点？
现实世界里有什么相关背景？
```

要求：

- 必须标注和原文的关系。
- 必须区分外部来源、模型推测和待验证内容。
- 可以更有启发性，但不能伪装成原文事实。

### 7.3 Add 模式

Add 模式允许用户给地图增加自己的评论和批注。

用户可以：

- 对节点写评论。
- 标记“重要”“存疑”“待复查”。
- 添加标签。
- 写下问题。
- 收藏节点。
- 把批注设为私有、团队可见或随分享链接公开。

Add 内容必须和系统生成内容分层展示，不能污染 Explain 结论。

### 7.4 分享模式

分享模式是生成好的地图的只读版本。

被分享者可以：

- 打开完整视觉地图。
- 点击节点继续下钻。
- 查看 Explain 引用。
- 查看公开 Explore 内容。
- 查看被发布者选择公开的批注。

被分享者不能：

- 修改原地图。
- 增加批注。
- 删除节点。
- 重新生成结构。
- 改写分享者的阅读路径。

分享模式的核心价值是把地图变成一个可传播的互动知识页面，而不是一张静态图片。

## 8. MVP 范围

### 8.1 第一版输入

第一版优先支持：

- 文章 URL。
- 粘贴正文。

暂缓支持：

- 视频。
- PDF。
- 论文。
- GitHub 仓库。
- 播客。

### 8.2 第一版功能

- URL 后拼链接或 query 参数直接生成地图。
- 无链接时进入输入框模式。
- 自动抓取和解析文章内容。
- 抽取标题、摘要、章节、核心论点和关键证据。
- 生成一个完整视觉总览画面。
- 支持点击节点下钻。
- 支持 Explain / Explore / Add 三种编辑态模式。
- 支持用户添加评论、批注、标签和疑问。
- 支持生成只读分享链接。
- 分享链接可以下钻查看，但不能编辑。

### 8.3 第一版体验标准

- 打开后第一视觉必须是画布，不是网站首页。
- 地图必须有完整渲染效果，而不是普通列表。
- 关键文字必须使用真实 DOM 或 Canvas 文本渲染，不能完全依赖图片模型。
- 每个 Explain 节点必须有来源引用。
- Explore 节点必须标注外部性或待验证状态。
- 分享模式必须明确只读。

## 9. 用户流程

### 9.1 URL 直达流程

```text
用户复制一篇文章链接
  -> 拼到 VisualMap URL 后或作为 url 参数
  -> 打开 VisualMap
  -> 系统自动解析
  -> 视觉画布逐步生成
  -> 用户点击节点下钻
  -> 用户切换 Explain / Explore / Add
  -> 用户保存或分享
```

### 9.2 输入框流程

```text
用户打开 VisualMap
  -> 看到全屏空画布和中心输入框
  -> 粘贴链接或正文
  -> 输入框收缩
  -> 地图从中心展开
  -> 用户开始点击探索
```

### 9.3 分享查看流程

```text
用户打开分享链接
  -> 直接看到发布者生成的完整地图
  -> 可以点击节点下钻
  -> 可以查看公开引用和公开批注
  -> 不能编辑、批注或重新生成
```

## 10. 信息架构

```text
Map
  -> Source
  -> Node
  -> View
  -> Trail
  -> Annotation
  -> Share Snapshot
```

### 10.1 Map

一次内容解析生成的完整地图。

### 10.2 Source

原始输入内容。

字段包括：

- URL。
- 标题。
- 作者。
- 发布时间。
- 正文。
- 抓取状态。
- 原始来源。

### 10.3 Node

地图中的知识节点。

字段包括：

- ID。
- 类型。
- 标题。
- 摘要。
- 所属模式。
- 来源引用。
- 父子关系。
- 视觉坐标。
- 可用操作。

### 10.4 Annotation

用户批注。

字段包括：

- ID。
- node_id。
- author_id。
- type。
- content。
- visibility。
- created_at。

### 10.5 Share Snapshot

分享快照。

字段包括：

- ID。
- source_map_id。
- permission。
- can_drill_down。
- can_add_annotation。
- included_annotation_visibility。
- created_at。

## 11. 技术方案

推荐架构：

```text
URL 入口层
  -> 内容输入层
  -> 抓取 / 解析 / 清洗层
  -> LLM 结构化理解层
  -> 节点图谱层
  -> 视觉布局层
  -> 视觉渲染层
  -> 引用与校验层
  -> 批注层
  -> 分享快照层
  -> 缓存层
```

关键原则：

- 先生成结构化节点，再生成视觉布局。
- 视觉效果服务于理解，不直接决定事实结构。
- 关键文字用 DOM 或 Canvas 文本渲染。
- 背景、纹理、插画、空间感可以更 fancy。
- 点击区域由结构化 hotspot 控制，不能靠图片模型猜测。
- 分享快照需要冻结结构和权限。

## 12. 视觉方向

VisualMap 应该避免传统网站感。

应该像：

- 一张可探索的知识海报。
- 一个研究任务的控制台。
- 一个视觉化的思维地图。
- 一个轻量的沉浸式信息空间。

不应该像：

- SaaS 官网。
- 文档站。
- 博客文章页。
- Chatbot 页面。
- 普通 dashboard。

视觉关键词：

- full-screen。
- rendered canvas。
- cinematic。
- layered。
- spatial。
- interactive poster。
- glowing focus。
- rich but readable。

## 13. 权限规则

### 13.1 创建者

创建者可以：

- 重新生成地图。
- 编辑标题。
- 添加批注。
- 删除自己的批注。
- 保存路径。
- 生成分享链接。
- 选择哪些批注公开。

### 13.2 分享访问者

分享访问者可以：

- 查看地图。
- 点击下钻。
- 查看公开内容。

分享访问者不能：

- 编辑地图。
- 添加批注。
- 重新生成。
- 改变分享快照。

## 14. 成功指标

### 14.1 体验指标

- 用户打开后 3 秒内理解这是一个视觉地图，而不是普通网页。
- 用户能在不阅读说明的情况下点击第一个节点。
- 用户能区分 Explain、Explore 和 Add。
- 被分享者能成功下钻查看至少 2 层内容。

### 14.2 产品指标

- URL 直达生成成功率。
- 首张地图生成完成率。
- 节点点击率。
- Explain 引用查看率。
- Explore 切换率。
- Add 批注创建率。
- 分享链接打开率。
- 分享链接下钻率。

## 15. 主要风险

- 画面太 fancy 导致信息不可读。
- 生成速度太慢，影响 URL 直达体验。
- Explore 内容被误认为原文事实。
- Add 批注和系统内容混在一起，破坏可信度。
- 分享权限不清，导致用户误以为别人能编辑。
- 内容抓取失败导致入口体验中断。

## 16. 结论

VisualMap 的关键不是做一个“更漂亮的网站”，而是做一个“打开即进入内容画面”的视觉知识地图。

产品入口要轻：链接可以直接拼到 URL 后生成，没链接时才进入输入框模式。

产品画面要重：第一视觉是完整、fancy、可点击、可下钻的渲染地图。

产品信任要稳：Explain 保证来源，Explore 保证边界，Add 保证个人沉淀，分享模式保证只读传播。
