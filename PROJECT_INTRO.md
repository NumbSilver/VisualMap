# VisualMap 分享文档

## 1. 背景：AI 内容浏览正在从“回答”走向“空间”

过去一段时间，AI 产品最常见的交互形态是聊天框。用户提出问题，模型返回一段文本，用户继续追问。这种方式足够直接，也很适合任务型场景，但它有一个明显限制：当内容变复杂之后，用户只能在一条线性的对话流里上下滚动。

可很多知识并不是线性的。一个概念会有背景、定义、例子、争议、类比、影响、后续问题。用户真正想要的不是只读完一段答案，而是可以在一片知识空间里选择方向、放大细节、发现新问题。

这也是 Flipbook、Infinite Browser 这类产品形态吸引人的地方。它们都在尝试把 AI 从“生成一段内容”推进到“生成一个可以浏览和探索的界面”。

## 2. Flipbook：把问题变成一张可探索的信息图

Flipbook 的核心体验非常直观：用户输入一个问题、主题，或者上传一张图片，系统不会返回传统网页，也不会返回一长段聊天答案，而是直接生成一张完整的信息图。

这张图本身就是页面。它包含标题、关键概念、图解、箭头、注释、局部说明和视觉层级。用户可以像看一张知识地图一样理解主题，也可以点击图里的任意区域继续下钻。

它最打动人的地方不是“生成了一张图”，而是“图变成了浏览界面”。用户不再通过链接列表进入内容，也不再通过聊天追问一层层展开，而是在一个视觉空间里点击自己感兴趣的位置。

Flipbook 的几个关键亮点：

1. 第一屏就是结果，不像传统网站需要导航、列表和入口页。
2. 生成内容是完整视觉画面，而不是文本块加图片。
3. 点击图片任意位置可以继续探索。
4. 每一次探索都像进入下一张更具体的信息图。
5. 视觉风格统一，形成一种连续的知识旅程。

从产品角度看，Flipbook 代表的是一种 image-first interface：图片不只是内容的补充，而是主要交互载体。

## 3. Infinite Browser：浏览器从“打开网页”变成“生成页面”

Infinite Browser 可以理解为更大的产品范式变化。传统浏览器的逻辑是：世界上已经有网页，用户通过地址栏、搜索或链接去访问它们。浏览器负责展示已有页面。

而 Infinite Browser 的逻辑是：页面可以被即时生成。用户输入一个意图，系统直接生成一个可浏览、可交互、可继续扩展的页面。

这会带来几个重要变化：

1. 浏览入口从 URL 变成自然语言、图片、问题或任意上下文。
2. 页面不再是固定文档，而是可以跟随用户兴趣继续生长。
3. 用户不是在搜索结果里选择内容，而是在生成的内容空间中探索。
4. 浏览器不只是展示工具，而变成了认知界面的生成器。

这个方向很有想象力。因为它把 AI 浏览器从“帮我找网页”升级成了“帮我生成一个我现在需要的页面”。

## 4. VisualMap：一个开源的视觉知识浏览器实验

VisualMap 是基于上述思路做的开源实验项目。它的目标是把一段文档、一个链接，或者一句问题，转成一张可以点击下钻的视觉知识地图。

它不是传统网站，也不是普通思维导图。用户打开后看到的应该是一张完整渲染好的视觉画面。输入可以有两种方式：

1. 直接把链接拼在 URL 后面，页面自动生成。
2. 打开后在输入框中输入链接、文档或问题，再生成视觉图。

VisualMap 想解决的核心矛盾是：

用户既想要可信，又想要延展性和惊喜感。

所以产品里设计了几种模式：

- Explain：基于原始内容稳定、可验证地下钻。
- Explore：在原始内容之外探索相关可能性、外部知识、类比和争议。
- Add：允许用户增加自己的评论、批注和理解。
- Share：生成一个不可编辑、但可以继续下钻查看的分享版本。
- Auto：自动判断当前应该走 Explain 还是 Explore。

其中 Auto 很重要。用户不应该每次都手动判断自己是在解释还是探索。系统可以根据输入内容和下钻深度自动选择：

- 如果用户输入的是一句短问题，更像搜索，就走 Explore。
- 如果用户输入的是长文档或文章链接，就优先走 Explain。
- 如果连续下钻后原文已经没有更多可解释内容，就自动切到 Explore。

## 5. 当前实现方案：先用 Flipbook-style direct generation

VisualMap 当前采用两阶段技术路线。

第一阶段是 Flipbook-style direct generation。也就是尽量用直接生图的方式复现 Flipbook 的体验。

流程如下：

1. 用户输入 URL、文本或问题。
2. 系统解析 URL 内容，提取标题、正文和摘要。
3. Text Provider 生成页面规划，包括标题、摘要、视觉提示词和节点结构。
4. Image Provider 生成一张完整的手绘信息图。
5. 用户点击图中某个位置。
6. 系统记录点击坐标，裁剪父图中对应区域，并加上标记。
7. Image Provider 基于这张局部参考图生成下一层更聚焦的 detail plate。
8. 每次生成结果保存成一个 Product，后续可以做分享和回放。

这种方案的优点是实现速度快、视觉冲击力强、接近 Flipbook 的第一感觉。缺点是文字准确性、点击语义、风格稳定性都依赖模型能力，需要不断调 prompt 和交互策略。

## 6. 第二阶段：如果直接生图不稳定，走确定性 VisualMap

如果第一阶段测试下来不够稳定，VisualMap 会进入第二阶段：确定性渲染方案。

第二阶段的核心思路是：不要让图片模型负责所有东西。文字、节点、箭头、引用、层级、批注这些需要准确的部分，由结构化数据和前端渲染来完成；图片模型只负责插画、纹理、背景或局部视觉素材。

这样可以解决几个问题：

1. 文字不会被图片模型画错。
2. 节点可以编辑、复制、引用和分享。
3. 下钻路径可以精确管理。
4. 分享页可以稳定复现。
5. Explain 模式可以绑定原文证据。

这会牺牲一部分“魔法感”，但会换来更高的可靠性和可控性。

## 7. 为什么要抽象 Provider

VisualMap 的目标是开源，所以模型调用不能写死在业务逻辑里。

现在项目把模型能力抽象成 Provider：

- Text Provider：负责大纲提取、页面规划、Explain/Explore 判断。
- Image Provider：负责生成整张视觉图，或基于父图做局部编辑。
- Vision Provider：后续可以用于识别用户点击的具体图像区域。

开源用户可以使用任何 OpenAI-compatible Provider，也可以接自己的本地模型、商业模型或内部模型。项目本身只保留通用接口，不绑定具体厂商。

## 8. 当前已经实现的能力

项目目前已经完成初版闭环：

- URL 或主题输入生成视觉图。
- 支持 `?url=` 直接生成。
- WeChat 文章内容解析。
- Auto / Explain / Explore / Add 模式。
- 点击任意图片区域下钻。
- 下钻时会裁剪并标记点击区域。
- Back / Forward 历史导航。
- Hide 按钮隐藏所有 UI，只保留图片。
- 父图上显示下钻编号标注。
- 每次生成自动保存成 Product。
- OpenAI-compatible Provider 抽象。
- MIT License 和开源 README。

## 9. 当前效果和主要差距

当前版本已经能跑通完整体验，但距离理想的 Flipbook 质感还有差距。

主要差距包括：

1. 视觉风格还需要继续稳定到浅色手绘信息图。
2. 第二层和第三层有时仍然像重新生成一张图，而不是明显下钻。
3. 点击位置还没有经过 Vision 模型做语义识别。
4. 生成速度还是同步等待，体验不够轻。
5. 分享模式还没有完整做成 `/n/:id` 的只读页面。
6. Add 批注模式还只是产品设计，没有完整交互实现。

下一步最重要的是提升“下钻感”。用户点击某个区域后，下一张图必须明显像在看这个区域的局部细节，而不是另一张同主题概览。

## 10. 后续路线

短期优先级：

1. 增加 `/n/:productId` 分享页。
2. 增加 `fast / balanced / pro` 生成档位。
3. 增加图片上传作为起点。
4. 引入 Vision Provider 识别点击区域。
5. 优化下钻 prompt，让每一层更有细节递进。

中期目标：

1. 做 Product 历史和探索树。
2. 完整实现 Add 批注模式。
3. 支持分享页只读下钻。
4. 做异步生成和加载状态。
5. 增加缓存和复用，降低重复生成成本。

长期目标：

1. 从 direct image generation 演进到确定性 VisualMap renderer。
2. 支持可编辑文字、引用证据、结构化节点。
3. 让 VisualMap 成为一个真正的视觉知识浏览器。

## 11. 一句话总结

VisualMap 想探索的是一种新的 AI 产品形态：不是聊天框，不是传统网页，也不是普通思维导图，而是一个可以从任意内容生成、可以点击下钻、可以标注分享的视觉浏览器。
