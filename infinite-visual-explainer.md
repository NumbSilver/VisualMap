# Infinite Visual Explainer 方案初稿

## 1. 它的作用

Infinite Visual Explainer 是一个面向长内容的“无限视觉解析器”。

用户输入一篇文章、一个视频链接、一份 PDF、一篇论文、一个播客，甚至一个代码仓库后，系统会先理解原始内容，再生成一张可点击的视觉总览页。用户点击总览页中的不同区域后，可以继续进入更细的解释页、证据页、原文页或相关背景页。

它的核心价值不是简单总结，而是把线性的内容变成一个可以探索的视觉空间。

传统阅读方式是：

```text
打开内容 -> 从头读到尾 -> 自己划重点 -> 自己建立理解结构
```

Infinite Visual Explainer 的方式是：

```text
输入内容 -> 自动理解结构 -> 生成视觉总览 -> 点击任意部分继续深入
```

它适合解决以下问题：

- 长文章太长，用户不知道先看哪里。
- 视频信息密度高，但时间轴浏览效率低。
- 论文、报告、深度文章需要先建立整体理解。
- 用户想快速知道“这篇内容讲了什么、为什么重要、细节在哪里”。
- 用户想像浏览地图一样浏览知识，而不是只看聊天框输出。

相比 Flipbook 原版，这个场景更实用，因为它有明确的原始输入。系统可以基于文章、视频转写稿、PDF 文本或代码内容来生成视觉解释，而不是完全依赖模型世界知识凭空生成。因此它更容易控制事实准确性，也更容易加入引用、来源和校验机制。

这里真正要解决的核心矛盾是：用户既想要可信，又想要延展性和惊喜感。

如果产品只做可信解释，它会变成一个更漂亮的摘要工具；如果产品只做开放探索，它又会变成一个容易漂移、难以验证的生成玩具。因此产品需要把“解释”和“探索”拆成两个明确模式：

- Explain 模式：基于原始内容，稳定、可验证地往下钻。
- Explore 模式：在原始内容之外，往相关可能性、延展信息、类比、争议和外部世界探索。
- Add 模式：用户在地图节点上增加自己的评论、批注和理解。
- 分享模式：把生成好的图发布成只读版本，其他用户不能编辑，但可以继续下钻查看。

这些模式不是风格差异，而是不同的产品状态。Explain 和 Explore 解决“系统如何生成内容”，Add 解决“用户如何留下自己的理解”，分享模式解决“内容如何被别人安全消费”。Explain 负责让用户知道“原文到底说了什么”，Explore 负责让用户发现“这件事还能连接到哪里”，Add 负责把阅读变成个人知识沉淀，分享模式负责让地图变成可传播的知识页面。

## 2. 方案

这个产品可以理解为：

```text
Perplexity + Pinterest + HyperCard + 可视化 RAG
```

它不是纯粹的生图工具，也不是传统阅读器，而是一个“内容理解 + 结构化拆解 + 视觉浏览”的系统。

### 核心流程

```text
用户输入内容
  -> 内容抓取 / 转写 / 解析
  -> 结构化理解
  -> 生成知识节点树
  -> 生成第一张视觉总览图
  -> 用户选择 Explain / Explore / Add
  -> 用户点击图中区域
  -> 进入对应解释节点、探索节点或批注节点
  -> 继续生成下一层视觉解释页
  -> 保存或发布为分享模式
```

### 页面层级

一篇文章可以被拆成这样的探索路径：

```text
摘要页
  -> 章节页
  -> 核心论点页
  -> 证据页
  -> 原文引用页
  -> 相关背景页
```

在实际产品里，同一个节点应该有两个可切换的下钻方向：

```text
当前节点
  -> Explain: 原文依据 / 概念解释 / 论证链 / 关键细节 / 可验证引用
  -> Explore: 背景知识 / 类比案例 / 反方观点 / 现实影响 / 后续问题
  -> Add: 我的评论 / 我的批注 / 我的疑问 / 我的标签 / 我的收藏
```

例如用户点击文章里的“核心论点”：

- 在 Explain 模式下，系统展示这个论点来自哪些段落、用了哪些证据、推理链是否完整。
- 在 Explore 模式下，系统可以展示相似案例、历史背景、业内争议、相关概念和可继续追问的问题。
- 在 Add 模式下，用户可以给这个论点加一句自己的判断、标记“重要/存疑/待复查”，或把它关联到自己的项目和笔记。

一个视频可以被拆成：

```text
视频总览页
  -> 时间线章节
  -> 关键观点
  -> 关键画面 / 关键帧
  -> 对应时间戳
  -> 详细解释
```

一个代码仓库可以被拆成：

```text
仓库总览页
  -> 模块关系
  -> 文件结构
  -> 核心流程
  -> 函数调用
  -> 具体代码解释
```

### 关键设计原则

第一，视觉页是入口，不是唯一真相。

页面可以用 AI 生成的信息图呈现，但每个视觉节点都必须绑定真实来源，例如原文段落、视频时间戳、PDF 页码、代码文件路径或外部引用。

第二，点击区域不应该完全依赖图像模型猜测。

更稳的做法是先生成结构化节点，再让图像模型根据节点生成视觉布局。前端保存每个区域的 hotspot 坐标。用户点击时，系统进入对应节点，而不是临时猜用户点到了什么。

第三，生图负责降低理解门槛，结构化内容负责保证准确性。

如果所有文字都由图片模型生成，容易出现错字、错位和幻觉。更好的方案是混合渲染：视觉背景、图表、插画可以生成；标题、引用、来源、时间戳、关键数字使用真实 DOM 文本叠加。

第四，产品要保留浏览路径。

每次点击都应该形成类似路径的结构，例如：

```text
/文章总览/核心论点/证据 2/原文片段
```

这样用户不会迷路，也可以返回任意上层节点。

第五，模式切换必须始终可见。

Explain / Explore / Add 应该是页面级的分段控制，而不是藏在 prompt 里的选项。用户每次点击节点时，都能清楚知道自己是在“原文内下钻”“原文外扩展”，还是在“留下自己的理解”。视觉上可以用来源状态区分：Explain 节点显示原文引用、页码、时间戳或代码位置；Explore 节点显示外部来源、推测强度和关联理由；Add 节点显示作者、时间、批注类型和可见范围。

第六，Explore 不能伪装成 Explain。

Explore 可以大胆、有趣、有启发，但必须标注它和原始内容的关系，例如“原文未提及，但与该概念相关”“这是一个类比”“这是一个争议视角”“这是外部资料补充”。这样产品既保留惊喜感，也不会破坏用户对系统的信任。

第七，分享模式必须是只读但可下钻。

分享出去的地图不应该暴露编辑能力，也不应该变成一张死图。被分享者可以点击节点、切换 Explain 和 Explore、查看引用和路径，但不能改原图、增加批注或重新生成结构。这样分享链接既像一篇可互动文章，也像一个受保护的知识快照。

## 3. 完整的内容

### 产品定位

Infinite Visual Explainer 是一个面向长内容、复杂信息和知识探索的视觉浏览器。

它的目标不是替代文章或视频本身，而是为它们生成一个可探索的解释层。用户可以先获得整体框架，再按自己的兴趣逐层深入。

更准确地说，它是一种多状态知识地图：

- Explain 模式让用户沿着原文结构稳定下钻，适合精读、核查、学习和复盘。
- Explore 模式让用户从当前节点向外扩展，适合启发、选题、研究准备和发现新问题。
- Add 模式让用户把自己的评论、批注、疑问和判断加到地图上，适合个人知识沉淀和团队讨论。
- 分享模式让地图变成只读但可下钻的互动页面，适合传播、汇报和异步阅读。

产品的默认承诺应该是：任何可信结论都能回到来源，任何开放延展都清楚标注边界，任何用户批注都和系统内容分层，任何分享链接都默认保护原图不被改写。

一句话定位：

```text
把任何长内容变成一张可以无限点击深入的视觉知识地图。
```

### 整体产品形态

它不应该长得像传统聊天工具，也不应该只是一个图片瀑布流。更合适的形态是“视觉知识地图工作台”：

```text
左侧：内容与路径
中间：可点击视觉地图
右侧：节点详情、引用、外部探索、个人批注
顶部：Explain / Explore / Add 模式切换、分享、保存
```

第一屏进入后，用户看到的不是一段回答，而是一张由原文生成的总览地图。地图上的每个节点都是一个可进入的知识对象，例如核心论点、关键人物、时间线、证据、概念、争议点或行动结论。

点击节点后，中间画布切换到下一层视觉页，左侧路径记录用户从哪里进入，右侧详情面板展示该节点的文本解释、引用证据和下一步建议。用户可以像浏览地图一样不断深入，也可以随时回到上层。

产品里最重要的对象有四类：

- Source：原始内容，包括文章、视频转写、PDF、论文、播客或代码仓库。
- Node：从 Source 中抽取出的知识节点，是地图的基本单位。
- View：某个 Node 的视觉表达，可以是图谱、时间线、流程图、对比图或证据板。
- Trail：用户走过的路径，可以保存、分享，也可以作为下一次继续阅读的入口。
- Annotation：用户加在节点或路径上的评论、批注、标签、问题和个人判断。
- Share Snapshot：某一刻发布出去的只读地图版本，保留可点击下钻能力，但冻结编辑和再生成权限。

Explain 模式下，右侧详情面板优先展示“可验证信息”：原文片段、段落位置、证据链、术语解释、上下文和确定性等级。它像一个有引用的视觉精读器。

Explore 模式下，右侧详情面板优先展示“相关可能性”：外部背景、相似案例、反方观点、类比、进一步问题和推荐阅读。它像一个从当前内容出发的研究雷达。

Add 模式下，右侧详情面板变成个人批注区。用户可以给节点加评论、打标签、写疑问、标记存疑或收藏，也可以把自己的批注设为私有、团队可见或随分享链接公开。

分享模式下，页面进入只读消费态。其他用户可以打开地图、沿着节点下钻、查看 Explain 证据、查看被公开的 Explore 内容和公开批注，但不能修改结构、删除节点、增加批注或触发新的生成。

一个理想的使用流程是：

```text
输入链接或正文
  -> 生成总览地图
  -> 用户先用 Explain 建立可信理解
  -> 对某个节点感兴趣时切到 Explore
  -> 用 Add 留下自己的批注和问题
  -> 发现外部线索后回到 Explain 核查原文
  -> 保存一条自己的阅读路径
  -> 发布成只读可下钻的分享版本
```

这让产品形成一个清晰的节奏：先稳住，再发散；先理解，再惊喜；先批注沉淀，再分享传播；先回到来源，再走向世界。

### 典型使用场景

#### 文章解析

用户输入一篇文章链接或粘贴正文。系统生成一张总览页，展示文章主题、核心观点、论证结构、关键案例和结论。

用户点击“核心观点”，进入观点拆解页；点击“证据”，进入引用和原文段落；点击“背景”，进入相关概念解释。

这是最适合作为 MVP 的场景，因为文章输入清晰、成本低、准确性更容易控制。

#### 视频解析

用户输入视频链接。系统先转写音频，提取章节、关键观点和重要时间戳，再生成视觉总览。

用户点击某个章节，可以看到该章节的摘要、关键帧、原话摘录和时间戳。它相当于把视频播放器的时间轴变成一张空间化知识地图。

#### 论文和报告解析

用户输入论文 PDF。系统提取研究问题、方法、实验、结论、局限性和引用关系。

用户可以点击“方法”看实验设计，点击“结果”看图表解释，点击“局限性”看批判性分析。

这个场景适合专业用户，但第一版实现成本略高。

#### 代码仓库解析

用户输入 GitHub 仓库链接。系统生成仓库结构图、核心模块图、调用链和关键文件解释。

这个方向和现有代码解释工具接近，但优势是可视化探索路径更强。

### MVP 建议

第一版建议只做文章解析。

MVP 功能包括：

- 输入文章链接或正文。
- 自动抽取标题、摘要、章节、核心观点和关键证据。
- 生成一张视觉总览图。
- 支持点击总览图中的节点进入下一层解释。
- 支持 Explain / Explore / Add 三个编辑态模式切换。
- Explain 节点必须展示原文引用和所在位置。
- Explore 节点必须展示关联理由、外部来源或“待验证”状态。
- Add 模式支持用户给节点添加评论、批注、标签和疑问。
- 每个解释页保留原文引用。
- 支持返回上级路径。
- 支持保存一个解析结果。
- 支持生成分享链接；分享版本不可编辑，但可以点击节点继续下钻查看。

第一版不建议做：

- 实时视频生成。
- 所有文字都生成在图片里。
- 完全无限开放式探索。
- 自动执行网页操作。
- 没有来源的事实扩展。
- 把 Explore 的内容混入 Explain 结论。
- 多人实时协同编辑批注。
- 分享后允许访问者改写原地图。

### 技术架构

推荐架构：

```text
前端交互层
  -> 内容输入层
  -> 抓取 / 解析 / 转写层
  -> LLM 结构化理解层
  -> 节点图谱层
  -> 视觉生成层
  -> 来源引用与校验层
  -> 缓存与分享层
```

核心数据结构可以是一个节点树：

```json
{
  "id": "root",
  "type": "summary",
  "title": "文章总览",
  "summary": "...",
  "mode": "explain",
  "trust_level": "source_grounded",
  "sources": [
    {
      "text": "原文片段",
      "location": "paragraph_3"
    }
  ],
  "children": [
    {
      "id": "argument_1",
      "type": "argument",
      "title": "核心论点 1",
      "available_modes": ["explain", "explore", "add"],
      "annotations": []
    }
  ]
}
```

Explain 节点和 Explore 节点可以共用同一套节点结构，但需要不同的约束：

```json
{
  "id": "node_42",
  "mode": "explore",
  "title": "相关争议",
  "relation_to_source": "原文提到该观点，但没有展开争议",
  "verification_status": "external_source_required",
  "sources": [],
  "external_sources": []
}
```

用户批注可以作为独立对象存储，避免污染系统生成的节点内容：

```json
{
  "id": "annotation_1",
  "node_id": "argument_1",
  "author_id": "user_123",
  "type": "comment",
  "content": "这个论点可以和我们正在做的选题关联。",
  "visibility": "private",
  "created_at": "2026-05-04T10:00:00Z"
}
```

分享版本可以存成一个快照：

```json
{
  "id": "share_1",
  "source_map_id": "map_1",
  "permission": "read_only",
  "can_drill_down": true,
  "can_add_annotation": false,
  "included_annotation_visibility": ["public"],
  "created_at": "2026-05-04T10:30:00Z"
}
```

视觉生成不应该直接决定信息结构。正确顺序应该是：

```text
先生成结构 -> 再生成视觉 -> 再绑定点击区域
```

### 和 Flipbook 的区别

Flipbook 的重点是“整个页面都是模型生成的像素”。它很有未来感，但问题是成本高、速度慢、事实不稳定。

Infinite Visual Explainer 更适合产品化，因为它不是凭空生成世界，而是解释一个已知内容。它可以保留 Flipbook 的探索体验，同时用真实来源降低幻觉风险。

核心差异：

```text
Flipbook: 生成一个视觉世界，让用户点击探索。
Infinite Visual Explainer: 解析一个真实内容，把它变成可点击的视觉知识地图。
```

### 产品风险

主要风险有六个：

- 事实错误：需要强制引用原文和来源。
- 生成成本：需要缓存每个节点，常见节点复用。
- 视觉不可读：关键文字应该用 DOM 渲染，不完全依赖图片模型。
- 用户迷路：必须有路径导航、历史记录和层级结构。
- 批注污染原文：用户 Add 内容必须和系统 Explain 内容分层展示。
- 分享权限不清：分享模式必须默认只读，并明确哪些批注会被带出去。

### 初步结论

这个方向比直接复刻 Flipbook 更值得做。

它继承了 Flipbook 最有价值的部分：通过点击视觉对象来探索信息；同时避开了 Flipbook 最脆弱的部分：完全依赖模型生成事实和界面。

最好的切入点不是“无限生成图片浏览器”，而是“长内容的无限视觉解析器”。

第一阶段建议聚焦文章解析，做出一个稳定、可信、可分享的视觉深读工具。等文章场景跑通后，再扩展到视频、论文、播客和代码仓库。
